{
  "name": "server",
  "version": "1.0.0",
  "description": "A simple server to serve the teeport frontend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve:local": "env-cmd -f .env.local node index.js",
    "serve:prod": "env-cmd -f .env.prod node index.js",
    "serve:lambda": "env-cmd -f .env.lambda node index.js",
    "serve": "env-cmd -f .env.prod node index.js"
  },
  "keywords": [
    "teeport"
  ],
  "author": "Zhe Zhang",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.7.4",
    "env-cmd": "^10.1.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
